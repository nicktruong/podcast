const routes = {
  // listener routes
  index: "/",
  login: "/login",
  search: "/search",
  signup: "/signup",
  playlist: "/playlist/:id",

  // podcaster routes
  pod: "/pod",
  podDashboard: "/pod/dashboard",
};

export default routes;
