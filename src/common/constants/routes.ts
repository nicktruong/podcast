const routes = {
  // listener routes
  index: "/",
  login: "/login",
  search: "/search",
  signup: "/signup",

  // podcaster routes
  pod: "/pod",
  podDashboard: "/pod/dashboard",
};

export default routes;
